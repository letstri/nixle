import type { AppOptions } from '../createApp';
import { type Route } from '..';
export declare const buildRoutes: ({ provider }: AppOptions, routerPath: string, routes: Route<any, any, any>[]) => void;
